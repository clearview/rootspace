server {
        listen 80 default_server;

        server_name _;

        root   /srv/web/build;

	      access_log off;

        location / {
                try_files $uri /index.html;
            }

}
